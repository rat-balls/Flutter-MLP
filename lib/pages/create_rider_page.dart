import 'package:flutter/material.dart';

class CreateRiderPage extends StatelessWidget {
  const CreateRiderPage({super.key});

  @override
  Widget build(BuildContext context) {
    // Ajoutez le contenu pour créer un cavalier ici
    return Scaffold(
      appBar: AppBar(
        title: Text('Création de Cavalier'),
      ),
      body: Center(
        child: Text('Formulaire de création de cavalier ici'),
      ),
    );
  }
}
